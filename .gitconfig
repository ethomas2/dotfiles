[user]
	email = evan@tempoautomation.com
	name = Evan Thomas

[diff]
	tool = nvimdiff

[difftool "nvimdiff"]
        cmd = "nvim -d \"$LOCAL\" \"$REMOTE\""


[diff "ansible-vault"]
    textconv = ansible-vault view
    cachetextconv = true

[core]
	editor = nvim
[merge]
	tool = nvimdiff
[alias]
        ch = checkout
        cho = checkout
        chout = checkout
        check = checkout

        c = commit
        co = commit
        com = commit

        b = branch
        br = branch
        bra = branch

        f = fetch
        fe = fetch

        p = push
        pu = push

        dt = difftool
        dtool = difftool
        difft = difftool
        dit = difftool

        d = diff

        mt = mergetool

        cp = cherry-pick

        r = rebase
        rb = rebase
        reb = rebase

        a = add
        ad = add
